# Backend SafeMatch - Ejemplo de variables de entorno
#
# Cópialo como ".env" dentro de la carpeta "backend" y ajusta valores.

# Entorno y puerto
NODE_ENV=development
PORT=3000

# MongoDB
# - Mongo local (instalado como servicio): localhost
# - Mongo con Docker Compose (desde /backend): el host interno suele ser "mongo"
MONGODB_URI=mongodb://localhost:27017/safematch

# CORS (frontend)
# Vite suele usar 5173; ajusta según tu caso.
CORS_ORIGIN=http://localhost:5173

# JWT (en producción CAMBIA estos valores)
JWT_SECRET=change_me_dev_secret
JWT_EXPIRES_IN=24h
JWT_REFRESH_SECRET=change_me_dev_refresh_secret
JWT_REFRESH_EXPIRES_IN=7d



